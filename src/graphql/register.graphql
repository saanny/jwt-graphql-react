mutation Register(
  $first_name: String!
  $last_name: String!
  $email: String!
  $password: String!
  $mobile: String!
) {
  register(
    input: {
      first_name: $first_name
      last_name: $last_name
      mobile: $mobile
      email: $email
      password: $password
    }
  )
}
